apiVersion: v1
kind: Pod
metadata:
  name: pythonapp
  labels:
    app: pythonapp
spec:
  containers:
    - name: pythonapp
      image: hub.docker.com/skdockers01/my-app:1.0
      ports:
        - containerPort: 8080
